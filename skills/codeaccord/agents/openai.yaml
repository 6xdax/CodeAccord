interface:
  display_name: "CodeAccord"
  short_description: "Explore, agree, implement, verify, and recover changes"
  default_prompt: "Use $codeaccord to explore this change through a read-only inspect-and-challenge loop, present the fixed Accord when ready, implement the confirmed scope, verify the result, and keep one recovery checkpoint when needed."

policy:
  allow_implicit_invocation: true
